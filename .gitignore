train/config_/
train/vint_train/data/sampler/
# Ignore all __pycache__ directories everywhere
**/__pycache__/
# Ignore all wandb directories everywhere
**/wandb/
# Model checkpoints (do not commit large weights, incl. omnivla-edge.pth)
*.pth
# Fine-tune output dirs
train/logs_frodo_lan_ft/
train/logs_frodo_lan_ft_full/
# frodo_lan LMDB cache (rebuilt from data)
omnivla_dataset/_cache/
# Raw dataset (do not commit ~32MB of images/pkl)
omnivla_dataset/
# generated visualization PNGs
train/eval_*.png
train/infer_*.png
# eval tool outputs (train/eval/): result folders + any loose PNGs (do not commit)
train/eval/results/
train/eval/*.png
# generated analysis/visualization PNGs at repo root (cmp_*, percase_*, swap_demo, grid_heatmap, ...)
/*.png
# all image / data blobs anywhere (datasets, not source) — jpg images + pickle labels
*.jpg
*.jpeg
*.pkl
*.pickle
# full dataset downloaded from HuggingFace (hoheng/omnivla_dataset): images + pkl + HF cache junk (.lock/.metadata)
omnivla_dataset_hf/
